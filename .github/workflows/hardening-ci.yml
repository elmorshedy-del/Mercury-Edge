name: Mercury Hardening CI

on:
  pull_request:
  push:
    branches:
      - paper-rigour-v2

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo hardening-ci-ok
